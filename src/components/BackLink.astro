---
import styles from './BackLink.module.css';

interface Props {
  href?: string;
  label?: string;
}

const { href = '/', label = 'Matthew Campagna' } = Astro.props;
---

<a href={href} class={styles.link}>← {label}</a>
